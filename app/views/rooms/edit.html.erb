<p><%= @room.name %>の編集ページ</p>

<%= form_with model: @room, local: true do |f| %>
  <div>
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :text, "text" %>
    <%= f.text_area :text %>
  </div>
  <div>
    <%= f.submit "更新する" %>
  </div>
<% end %>