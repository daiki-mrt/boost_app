<h1>ルーム新規登録</h1>

<%= form_with model: @room, local: true do |f| %>
  <div>
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :text, "text" %>
    <%= f.text_area :text %>
  </div>
  <div>
    <%= f.submit "作成する" %>
  </div>
<% end %>